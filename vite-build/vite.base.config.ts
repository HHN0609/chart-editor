import { defineConfig } from "vite";

const viteBase = defineConfig({
    css: {
        preprocessorOptions: {
            less: {
                
            }
        }
    }
});

export default viteBase;